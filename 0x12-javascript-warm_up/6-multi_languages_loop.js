#!/usr/bin/node

const arrayStrings = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
const arrayLength = arrayStrings.length;
let i = 0;

while (i < arrayLength) {
  console.log(arrayStrings[i]);
  i++;
}
